# Face-Recognition
In this, we can capture faces and store them in file system database and during video, it can identify multiple faces from file system database. It needs internet once to download model. After that, it needs no internet connection to work.

/process: It is used to store faces as image locally. It then compares those faces with video and identify faces.

/display: It captures video and label the faces with name.
